"use client";

import { FC } from "react";

interface accesscomponentProps {
  ticket: ITicket;
}
const Accesscomponent: FC<accesscomponentProps> = ({ ticket }) => {
  return <div>accesscomponent</div>;
};

export default Accesscomponent;
